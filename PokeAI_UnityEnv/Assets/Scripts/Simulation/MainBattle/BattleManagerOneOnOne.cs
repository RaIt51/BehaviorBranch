namespace Simulation.OneOnOne
{
    /// <summary>
    /// Control the battle sequence of OneOnOne. This doesn't control the ML aspects.
    /// </summary>
    public class BattleManagerOneOnOne : BattleManager
    {
        // Start is called before the first frame update
        void Start() { }

        // Update is called once per frame
        void Update() { }
    }
}
